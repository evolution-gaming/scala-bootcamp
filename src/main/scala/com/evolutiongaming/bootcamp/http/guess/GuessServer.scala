package com.evolutiongaming.bootcamp.http.guess

object GuessServer {
  // Write a server that supports 2 requests:
  //
  // POST /guess?left={left}&right={right}
  //  Makes a new random number in the range of [left..right) and
  //  returns an ID that can be used to guess this number.
  //
  // GET /guess/{id}?number={number}
  //  Takes a number and returns:
  //  - 'lower' if this number is lower than the guessed one
  //  - 'greater' if this number is greater than the guessed one
  //  - 'equal' if this number equals to the guessed one


}
